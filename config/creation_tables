

CREATE KEYSPACE test WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 3};


CREATE TABLE event (
                       globaleventid int,
                       date int,
                       actor1countrycode text,
                       actor2countrycode text,
                       goldsteinscale float,
                       actiongeocountrycode text,
                       PRIMARY KEY ((date),actiongeocountrycode)
                  );
                  


CREATE TABLE mentions (
                       globaleventid int,
                       language text,
                       PRIMARY KEY ((globaleventid),language)
                  );


CREATE TABLE kb (
                       date int,
                       source text,
                       theme text,
                       location text,
                       persons text,
                       tone float,
                       PRIMARY KEY ((source, date))
                  );

============== Update ###############"""

CREATE TABLE kb (
                       date int,
                       source text,
                       theme text,
                       location text,
                       persons text,
                       translation text,
                       score float,
                       positif float,
                       negatif float,
                       PRIMARY KEY ((source, date))
                  );
                  

CREATE TABLE event (
                       globaleventid int,
                       date int,
                       goldsteinscale float,
                       actiongeocountrycode text,
                       PRIMARY KEY ((date),actiongeocountrycode)
                  );



